#include<iostream>
#include<vector>
using namespace std;

// O(nk) O(n + k)
// len == the length of numbers in A.
void radix_sort(vector<int> A, int len) {
    for(int i = 0; i < len; ++i) {
        // use a stable sort to sort array A on digit i.

    }
}

